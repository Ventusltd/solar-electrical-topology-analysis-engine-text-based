# Quantum Spawn Progress Log

**Title:** Build 025.5 Resistance-Evidence Tranche All Green

**File:** `202607312220-build-025-5-all-green.md`

**Timestamp:** 2026-07-31 22:20 Europe/London

**Version:** 1.0

**Status:** Verified checkpoint

**Authority:** GitHub Actions run 30666027826 and artifact 8806995997

**Current Build:** Build 025.5D — Resistance Evidence Authority

## Final validation result

Both GitHub Actions jobs passed:

```text
validate                           PASS
Clean installed wheel authority    PASS
```

The consolidated execution envelope records:

```text
Python                             259 passed / 0 failed
V8 model                           13/13 golden tests
V8 authority reconciliation         6/6 tests
V9 deterministic engine            10 passed / 0 failed
V10 JavaScript                     13 passed / 0 failed
Clean installed wheel              PASS
```

## Clean-wheel authority result

```text
distribution version     0.4.0
authority status         canonical_candidate
migration stage          build-025.5-package-authority
comparison hash          sha256:413aa93c98d1b9fd5f9ffee4f577cfe29a6e74f48bd927a3ebe7f541545d6366
```

The installed reference comparison reproduced:

```text
sequential total conductor     2513.328 m
leapfrog total conductor       2560.128 m
field-installed reduction       798.288 m
factory-fitted increase          845.088 m
total conductor increase          46.800 m
```

The legacy compatibility names resolved to modules inside the installed `solar_topology/array` package rather than the repository root.

## Resistance-evidence result

The validated implementation now provides:

- controlled resistance-basis and value-kind vocabularies;
- deterministic resistance-evidence records and hashes;
- product-bound 4 mm² and 6 mm² resistance records;
- applied-evidence-set hashing isolated from unrelated registry growth;
- resistance source, revision, verification state and temperature-coefficient basis in calculation receipts;
- evidence propagation through exact and interval calculations;
- explicit downgrade of unregistered or overridden resistance values;
- V6 and V9 lower-bound ideal-bulk resistance warnings;
- unchanged topology input serialisation and geometry/topology hash domains.

## Remaining evidence limitation

The two generic standard-maximum conductor records retain:

```text
source revision: edition-not-yet-encoded
verification: standards_review_required
```

Therefore the numerical standard source still requires edition and table verification before certification use. This is visible in the receipts and is not treated as a completed standards conclusion.

## Validation probe closure

PR 13 was a branch-only validation probe containing one Markdown marker. It was closed without merge after the green result. The marker is not part of `main`.

## Working rule retained

Continue in small bounded steps:

1. one material change;
2. one validation result;
3. one Quantum Spawn progress entry;
4. no next material step until the current evidence is understood.

## Next single step

Do not begin dimensional geometry or Build 026 yet. First choose one narrow resistance follow-up: verify and encode the exact licensed standard edition/table reference, or add one genuine manufacturer-declared product record with a revision-controlled source.
