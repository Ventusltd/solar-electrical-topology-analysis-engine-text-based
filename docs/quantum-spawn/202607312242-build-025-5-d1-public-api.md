# Quantum Spawn Progress Log

**Title:** Build 025.5D1 Resistance Qualification Public API

**File:** `202607312242-build-025-5-d1-public-api.md`

**Timestamp:** 2026-07-31 22:42 Europe/London

**Version:** 1.0

**Status:** Verified checkpoint

**Authority:** GitHub Actions run 30667404095 and artifact 8807493690

**Current Build:** Build 025.5D1 — Resistance Source Qualification Gate

## Scope completed

This step exposed the already validated resistance-source qualification gate through the supported package API. It did not change resistance values, electrical calculations, topology hashes, calculation receipts or evidence-promotion rules.

The following names are now available directly from `solar_topology`:

```text
RESISTANCE_QUALIFICATION_SCHEMA_VERSION
ResistanceSourceAssessment
ResistanceSourceStatus
assess_resistance_source
```

## Public API status

All four names are explicitly classified as:

```text
provisional
```

They were not promoted to canonical status. The qualification gate is operational and externally usable, but its integration into authoritative calculation receipts has not yet occurred.

The public API manifest no longer relies on the unknown-name fallback for these exports.

## Consumer contract

A clean installed-wheel consumer can now:

```python
import solar_topology as api

assessment = api.assess_resistance_source(
    api.EXTERNAL_STRING_6MM2.resolved_resistance
)
```

The clean-wheel probe verifies that:

- the four names exist in `solar_topology.__all__`;
- each name is explicitly provisional;
- the top-level function and status type are the same objects as the packaged qualification module authority;
- imports resolve from the installed wheel rather than the repository checkout;
- both existing generic conductor records remain candidates;
- no generic record is silently promoted.

## Existing generic source result

Both generic standard-maximum conductor records continue to produce:

```text
status: candidate
reasons:
- SOURCE_REVISION_PLACEHOLDER
- VERIFICATION_NOT_VERIFIED
```

This is the correct result because the exact standard edition and table reference remain unencoded and the source state remains `standards_review_required`.

## Validation result

Both GitHub Actions jobs passed:

```text
validate                           PASS
Clean installed wheel authority    PASS
```

Artifact `8807493690` records:

```text
Python                             268 passed / 0 failed
V8 model                           13/13
V8 authority reconciliation         6/6
V9 deterministic engine            10/10
V10 JavaScript                     13/13
Clean installed wheel              PASS
```

## Commits

```text
4de665e  api: classify resistance qualification exports
1fae65e  api: expose resistance source qualification gate
426f482  test: add resistance qualification public API contract
0e2dec8  test: probe qualification gate from clean wheel
```

## Repository hygiene

Restore point:

```text
restore/2026-07-31-2234-pre-build-025-5-d1-api
```

Validation PR 15 contained one branch-only Markdown marker. It was closed without merge after the green result. The marker is not part of `main`.

## Working rule retained

Continue in bounded steps:

1. one material change;
2. one validation result;
3. one Quantum Spawn progress entry;
4. no next material step until the current evidence is understood.

## Next single step

Add deterministic serialisation for `ResistanceSourceAssessment`:

```text
assessment payload
canonical JSON
assessment hash
```

The hash shall bind the qualification schema, source record hash, status and reason codes. This next step shall remain independent of calculation receipts and shall not promote any source. Calculation-receipt integration must be a later, separately validated step.
