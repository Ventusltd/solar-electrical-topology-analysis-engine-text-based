# Repository AI entry point

For all V9 work, begin with:

`v9-sandbox/debug/CLAUDE.md`

The canonical V9 engineering logic is:

`v9-sandbox/debug/engine.js`

The canonical deterministic review suite is:

`v9-sandbox/debug/tests.js`

Do not place V9 engineering mathematics, topology decisions, cable calculations or warnings in browser rendering code. The browser must only collect inputs and display the canonical computation report.

Before adding graphical features, verify the mathematics and physical interpretation against the questions in the debug instructions.