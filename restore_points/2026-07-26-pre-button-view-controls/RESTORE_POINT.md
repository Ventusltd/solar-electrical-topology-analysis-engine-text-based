# Restore point — before button-only view controls

Created before removing gesture-based pan and zoom and adding deterministic view buttons.

Restore commit:

`1bc0d33715d989e42d379381a9fc8a5a58cdd1a6`

To recover, restore `index.html` and remove `navigation-buttons.js` and `navigation-buttons.css`, or restore the repository files from the commit above.

No branch is used. This folder is only a recovery record.
